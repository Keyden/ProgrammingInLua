--请编写一个函数，其参数为一个数组，返回值为数组中元素的所有组合。
--提示： 可以使用组合的递推公式 C(n,m) = C(n -1,m-1)+C(n -1, m）。
--要计算从 n 个元素中选出 m 个组成的组合 C(n,m），可以先将第一个元素加到结果集中，然后计算所有的其他元素的 C(n-1,m一 1）；
--然后，从结果集中删掉第一个元素， 再计算其他所有剩余元素的 C(n - I,m）。
--当 n 小于 m 时，组合不存在；当 m 为 0 时，只有一种组合（一个元素也没有）。